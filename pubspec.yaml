name: sdmx
description: Fetch, parse and filter through SDMX records
version: 1.0.0
repository: https://github.com/sohaibbg/sdmx

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
